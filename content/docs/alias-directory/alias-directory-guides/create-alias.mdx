---
title: Create a New Alias
---

This guide teaches you step-by-step how to create a new Alias by submitting an Anchor record to the Ledger. Creating an Anchor establishes the critical linkage between a user's wallet and a simple external identifier, enabling payment resolution.

## Pre-requisites

Before you start, ensure you have the following resources:

- **API Access:** You have the base URL for the Ledger's API (`/v2`).
- **Signer Setup:** You have an authenticated Signer identity and the required private key with permissions to perform creation (`POST`) operations on Anchor records.
- **Required Data:** You have the specific Alias Key, the Wallet ID, and the Payment Credential details you intend to link.

## Creating and Registering a new Alias

Follow these steps to successfully create and register a new Alias in the Directory:

1. **Define the Anchor Payload**
     Construct the JSON payload for the Anchor record. The payload must include the unique identifier, the target financial account, and the associated payment credentials.

    <Callout title="Anchor Creation Payload">
            ```json
            {
            "handle": "carlos6@minka.io",
            "alias_type": "email",
            "target": "wlt_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
            "account_data": {
                "account_number": "1234567890",
                "routing_code": "999"
            },
            "owner_data": {
                "name": "Carlos Jose Andino Nunez",
                "national_id": "1000000000"
            },
            "custom_fields": {
                // Add any region-specific or policy-required custom data here
            }
            }
            ```
    </Callout>

    You must define the following required fields in the request body:

        - `handle`: The unique, user-friendly Alias Key (e.g., +573001234567).
        - `alias_type`: Specifies the type of the alias (e.g., phone_number, email, national_id).
        - `target`: The unique identifier of the user's Wallet or account in the Ledger that the alias should direct to.
        - `account_data`: The detailed, private financial information (Payment Credentials) required to execute a payment.
        - `owner_data`: Any necessary Custom Data about the user (e.g., owner name, ID number).

2. **Sign and Submit the Anchor to the Ledger**
    The Ledger requires that every operation be cryptographically signed by an authorized Signer to prove both identity and data integrity.

    ### Submit using the Minka CLI
    The Minka CLI automatically handles signing the request with your configured Signer private key before transmission.
    
        1. Save the payload from Step 1 to a local file (e.g., anchor.json).
        2. Execute the minka anchor create command, referencing the payload file:

            ```bash
            # Assuming 'anchor.json' contains the payload from Step 1
            minka anchor create --payload-file ./anchor.json
            ```
    ### Submit using the API
    If you are not using the Minka CLI, you must manually sign the request payload from Step 1. Use your private key to generate a digital signature, and then send the signed payload to the Ledger using the endpoint `POST /v2/anchors`.
    
    <Callout type="warn">
    The Ledger will reject any request that is not signed by an authorized Signer or if the payload has been altered after signing.
    </Callout>

3. **Review the Successful Response**
    If the request is successful, the Ledger returns a `201 Created` status, and the new Anchor record is immediately available in the Active state for resolution by other participants.

    Send the signed payload to the Ledger using the endpoint `POST /v2/anchors`

        <Callout title="Example Response">
        ```json
        {
        "hash":"9084396379b9c1d48cb2a66847edef1c4f5441460d4f7e0e09c59abb5b98d7cb",
        "data":{
            "handle":"carlos6@minka.io",
            "wallet":"DICE",
            "target":"svgs:123458@lulobank.com",
            "symbol":"cop",
            "custom":{
                "aliasType":"tel",
                "documentType":"nidn",
                "documentNumber":"0801198607268",
                "accountType":"svgs",
                "accountNumber":"123457",
                "firstName":"Carlos",
                "secondName":"Jose",
                "lastName":"Andino",
                "secondLastName":"Nunez",
                "entityType":"individual",
                "targetSpbviCode":"SRV",
                "participantCode":"123123"
            },
            "access":[
                {
                    "action":"any",
                    "signer":{
                    "$record":"owner"
                    }
                }
            ],
            "schema":"person"
        },
        "meta":{
            "proofs":[
                {
                    "method":"ed25519-v2",
                    "public":"ak40/ebXaPHdSwypXWHDlFaZZx0zdajPBz/dijLvdaE=",
                    "digest":"2f34a270823568486be5fd4cf02d387821f5183b5b386ed7f6dcea94ffb45f7d",
                    "result":"Swk6tcVgBB4XrywOAWyzKhMLuegGsF6iEoJLXEnVcTM5XuWerzHOl2OrQB4Cq3gjRNwncPpxAEjrn5Noj89jBQ==",
                    "custom":{
                    "moment":"2025-04-14T18:16:51.994Z",
                    "domain":null,
                    "consentDate":"2025-04-14T14:23:45.123Z",
                    "received":"2025-04-14T14:23:45.123Z",
                    "dispatched":"2025-04-14T14:23:45.123Z",
                    "status":"created"
                    }
                }
            ],
            "labels":[
                "ndin:0801198607268"
            ]
        }
        }
        ```
        </Callout>

    <Callout>
    In regulated environments (e.g., Centralized Directory like DICE), the Ledger performs additional steps, including third-party validation and adding compliance-required Timestamps to the record before completion.
    </Callout>